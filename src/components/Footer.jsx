import React, { Component } from 'react'
import { Button } from '@material-ui/core'

class Footer extends Component {
  render () {
    return (
      <footer>
        <Button variant="contained" color="primary">GENERATE CHART</Button>
      </footer>
    )
  }
}

export default Footer
